 git remote: 查看当前本地仓库跟远程库是否关联；
		       a) git remote rm origin 移除与远程库的关联；
		       b) git remote add origin git@github.com:userName/cangku.git;
		       c) git push -u origin / git push origin master;